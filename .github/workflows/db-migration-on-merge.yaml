name: 🔥 Migrate database on merge

on:
  push:
    branches:
      - main

jobs:
  migration:
    name: 🔑 Migrate database
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Download deps
        run: npm ci
